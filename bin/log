#!/usr/bin/env bash

if [[ -z $1 ]] || [[ -z $2 ]]; then
  printf "\nusage: %s 'info' 'message to send'\n\n" "$0"

  exit 1
fi

readonly LEVEL="$1"
readonly MESSAGE="$2"

node -e "$(printf 'console.log("\x1b[32m%s\x1b[0m:\t%s")' "$LEVEL" "$MESSAGE")"
